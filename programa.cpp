#include <iostream>
#include <string.h>
#define Max char[0..1000];

using namespace std;

int main() {
	int N, opendiamantes, cont, i;
	char v[100];

	for (i = 0; i <= 1000; i++) {

		cout << " INFORME O VALOR A SER TESTADO: " << endl;                                //impressão
		cin >> N;
	}

	for (i = 0; i <= 1000; i++) {
		if ((v[i] == ('<')) & (opendiamantes == 0)
			opendiamantes = 1;

		else
			if ((v[i] == ('>'))
				cont = cont++;
				opendiamantes = o;
				cout << " cont: " opendiamantes << endl;

	}
	return 0;

}